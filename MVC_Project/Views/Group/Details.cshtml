@model IvanSusaninProject_Contracts.BindingModels.GroupBindingModel
@{
    ViewData["Title"] = "Детали группы";
}

<h2>Детали группы</h2>

<dl class="row">
    <dt class="col-sm-3">Название</dt>
    <dd class="col-sm-9">@Model.Name</dd>

    <dt class="col-sm-3">Количество</dt>
    <dd class="col-sm-9">@Model.HumanAmount</dd>
    <dt class="col-sm-3">Состав</dt>
    <dd class="col-sm-9">@Model.HumanType</dd>
</dl>
<div class="d-flex gap-2 mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Редактировать</a>
    <a asp-action="Index" class="btn btn-secondary">Назад к списку</a>
    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Удалить</a>
</div>
